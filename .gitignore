# Rust
/target
**/*.rs.bk

# wasm-pack output (generated into the web app)
/crates/gnss-wasm/pkg
/web/src/wasm

# Node / Next.js
node_modules
/web/.next
/web/out
/web/next-env.d.ts
/web/tsconfig.tsbuildinfo
.pnpm-store

# Ephemeris fetched on demand by /api/ephemeris and cached on disk
/data/cache

# IQ job records and their outputs. Generated artefacts, and the binaries run
# to hundreds of megabytes each.
/data/jobs

# Python
__pycache__/
*.pyc

# Env
.env*.local

# OS
.DS_Store

# Claude Code local permission grants and runtime state (session-specific)
.claude/settings.local.json
.claude/scheduled_tasks.lock
